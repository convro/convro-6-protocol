import SwiftUI

// MARK: - Color + Theme
extension Color {
    // MARK: - Brand Colors
    static let convroBlue = Color("ConvroBlue")
    static let convroGreen = Color("ConvroGreen")
    static let convroRed = Color("ConvroRed")

    // MARK: - Message Bubbles
    static let sentMessageBackground = Color("SentMessageBackground")
    static let receivedMessageBackground = Color("ReceivedMessageBackground")

    // MARK: - Backgrounds
    static let chatBackground = Color("ChatBackground")
    static let inputBackground = Color("InputBackground")
}
